"""
Backtesting pipeline for the Higher High Breakout strategy: historical
data fetch, the ported Pine Script logic, a bar-by-bar simulation
engine, regime tagging, and metrics — reusing the same brokers, regime
classifier, and config the live bot already uses.
"""
