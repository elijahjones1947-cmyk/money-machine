// Stub — no earnings calendar data source wired up yet (Phase 3 lists
// get_upcoming_earnings as a Hermes tool to build). Placeholder so the
// widget slot/tray system is complete and ready to receive real data.
export function EarningsWidget() {
  return <div className="empty-state">Earnings calendar coming soon</div>;
}
