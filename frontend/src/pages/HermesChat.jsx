export function HermesChat() {
  return (
    <div>
      <h2>Hermes</h2>
      <div className="empty-state">
        The Hermes agent isn't wired up yet — this page is a placeholder for its full-page chat,
        landing here once the Phase 3 backend (tools + Anthropic API key) is live.
      </div>
    </div>
  );
}
